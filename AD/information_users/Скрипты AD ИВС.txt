Админы ИВС (инвентаризация УЗ с привелегиями без смаркарты):
Get-ADUser -Filter {AdminCount -eq 1 -AND Enabled -eq "True" -AND SmartcardLogonRequired -eq "False" -AND ObjectClass -eq "user"} -server adm.gazprom.ru -Properties UserPrincipalName,Name,PasswordLastSet,LastLogonDate,Description,TrustedToAuthForDelegation | Select-object UserPrincipalName,Name,PasswordLastSet,LastLogonDate,Description,TrustedToAuthForDelegation | Export-CSV -Force -NoTypeInformation Z:\!!!_path_!!!\export_admin_ivs.csv -Delimiter ',' -Append -Encoding UTF8
=========================================================================
